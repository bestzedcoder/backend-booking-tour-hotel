spring:
  application:
    name: Booking Tour & Hotel
  profiles:
    active: dev
  security:
    user:
      name: username

# port
server:
  port: 8080
#prefix endpoint
  servlet:
    context-path: /api/